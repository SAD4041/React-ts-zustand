// export type IconName = "paw" | "chat" | "shield" | "star" | "headset";

export type Feature = {
  icon: string;
  title: string;
  text: string;
};

export type TeamMember = {
  name: string;
  img?: string;
};
